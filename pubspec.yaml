name: digitor
description: Production Flutter UI for DigitorEngine.
publish_to: "none"
version: 0.3.0+7

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  digitor_engine_ffi:
    git:
      url: https://github.com/Tajuli/DigitorEngine.git
      path: dart/digitor_engine_ffi
      ref: 63893bd2b4f03ae8f813768f76691e91db022af8

  file_selector: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
