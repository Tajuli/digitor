name: digitor
description: Production Flutter UI for DigitorEngine.
publish_to: "none"
version: 0.3.0+7

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  digitor_engine_ffi:
    git:
      url: https://github.com/Tajuli/DigitorEngine.git
      path: dart/digitor_engine_ffi
      ref: bfffb2e21e767155b7d570cfe495cc88a2309d43

  file_selector: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
